import { FormatPlayerImgPipe } from './format-player-img.pipe';

describe('FormatPlayerImgPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPlayerImgPipe();
    expect(pipe).toBeTruthy();
  });
});

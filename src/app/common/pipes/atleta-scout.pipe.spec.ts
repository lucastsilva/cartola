import { AtletaScoutPipe } from './atleta-scout.pipe';

describe('AtletaScoutPipe', () => {
  it('create an instance', () => {
    const pipe = new AtletaScoutPipe();
    expect(pipe).toBeTruthy();
  });
});
